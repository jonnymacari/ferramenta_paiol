{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements_prod.txt"
  },
  "deploy": {
    "startCommand": "gunicorn -b 0.0.0.0:$PORT camp_project.wsgi:application"
  }
}
